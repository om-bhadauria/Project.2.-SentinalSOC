FROM python:3.10-slim
WORKDIR /app
COPY ai/behavior/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY ai/behavior/ ./
EXPOSE 8001
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8001"]
